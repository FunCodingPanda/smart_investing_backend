const knex = require('../db')

createUser = (user, fn) => {
  return {error: 'Not implemented yet'}
}

getAll = (fn) => {
  return {error: 'Not implemented yet'}
}

getById = (id, fn) => {
  return {error: 'Not implemented yet'}
}

module.exports = {
  createUser,
  getAll,
  getById
}
